---
# Local inventory for AWS API-driven deployments.
# The playbooks run on the control node and use boto3 to call AWS services.
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
